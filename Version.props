<Pr    <!-- Main Package Version - Update this single place to change version across all projects -->
    <HalaDatabaseAgentVersion>1.0.18</HalaDatabaseAgentVersion>
    
    <!-- Assembly Version Properties -->t>
  <!-- Central Version Management for Hala.DatabaseAgent.OracleMcpServer -->
  <PropertyGroup>
    <!-- Main Package Version - Update this single place to change version across all projects -->
    <HalaDatabaseAgentVersion>1.0.16</HalaDatabaseAgentVersion>
    
    <!-- Assembly Version Properties -->
    <AssemblyVersion>$(HalaDatabaseAgentVersion).0</AssemblyVersion>
    <FileVersion>$(HalaDatabaseAgentVersion).0</FileVersion>
    <InformationalVersion>$(HalaDatabaseAgentVersion)</InformationalVersion>
  </PropertyGroup>
</Project>
